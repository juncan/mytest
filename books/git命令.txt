git clone https://gitee.com/iBase4J/iBase4J.git

上传资料
//忽略文件上传
touch .gitignore
1、更新下本地项目
git pull origin master

2、git add .

3、git commit -a -m "代码提交"

4、git push origin master


1、查找域名对应的ip地址，并修改hosts文件

nslookup github.global.ssl.fastly.Net
nslookup github.com 

查看远程git地址
git remove -v